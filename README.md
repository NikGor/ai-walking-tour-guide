# Solaris Pliny

Location-aware AI historian bot named after Pliny the Elder — Roman historian and naturalist. Send GPS coordinates (+ optional photo) and get back a rich historical narrative about where you are.

## Run

```bash
docker-compose up --build
```

Or locally:

```bash
poetry install
python main.py
```

API: `POST /chat`
